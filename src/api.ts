import CompressionType from "./compression-type";
import compressBuffer from "./compress";
import decompressBuffer from "./decompress";

export {
  CompressionType,
  compressBuffer,
  decompressBuffer
};
